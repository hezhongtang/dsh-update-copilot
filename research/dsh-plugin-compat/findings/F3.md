# F3: GitHub Issues / PRs / 兼容讨论

## Findings

### [1] DeepSeek Harness 官方 README 在 developer preview 阶段明确声明将有兼容性破坏变更，这是插件兼容问题的制度性根源。
- quote: "DeepSeek Harness is in _developer preview_ and iterating rapidly. __THERE WILL BE COMPATIBILITY-BREAKING CHANGES.__"
- url: https://github.com/deepseek-ai/deepseek-harness
- source_type: primary
- published: 2026-08-13 (repo created); README fetched 2026-09-22
- confidence: high

### [2] dsh-update-copilot #7 把升级炸插件归纳为四类重复模式：host 导出移除、0.x prerelease 下 peer 范围失效、pnpm 共享依赖重写连带损坏兄弟包、破坏性变更埋在 release notes 正文。
- quote: "1. **目标版本移除了插件正在 import 的 host 导出**（`settingsNamespace`/`installSettingsSection` 类，ESM 链接期直接炸整树）；2. **peer 范围在 0.x prerelease semver 下形同虚设**（`^0.1.0-rc.8` 不匹配 `0.1.2-rc.1`…）；3. **pnpm 更新共享依赖时连带改写"兄弟包"**…；4. **破坏性变更埋在 release notes 正文里**"
- url: https://github.com/hezhongtang/dsh-update-copilot/issues/7
- source_type: primary
- published: 2026-09-05
- confidence: high

### [3] 具体导出丢失案例：第三方插件 dsh-univer-office 引入了 `@deepseek-ai/dsh-settings` 不存在的运行时导出 `settingsNamespace`（仅类型导出），导致整棵插件树加载失败、GUI 端口不可达。
- quote: "The requested module '@deepseek-ai/dsh-settings' does not provide an export named 'settingsNamespace'"
- url: https://github.com/deepseek-ai/deepseek-harness/discussions/5363
- source_type: community
- published: unknown (around 2026-08/09, dsh-settings@0.1.2-alpha.3)
- confidence: high

### [4] 另一导出丢失案例：`installSettingsSection` 从 dsh-settings 移除后，单个第三方插件在 ESM 链接期失败即可中止整个应用启动（DSH boot 是 fail-loud），且无自修复路径。
- quote: "The requested module '@deepseek-ai/dsh-settings' does not provide an export named 'installSettingsSection'"
- url: https://github.com/deepseek-ai/deepseek-harness/discussions/5426
- source_type: community
- published: unknown (dsh 0.1.2-alpha.3 era)
- confidence: high

### [5] peer 范围在 prerelease 语义下失效的实锤：`@michengai/dsh-archive-manager` 声明 peer `>=0.1.0-rc.5 <0.2.0` 只能命中 `0.1.0-rc.8`、永远选不中 `0.1.2-alpha.x`，旧形 `sessionProjectionCache`（无 `hydratePrepared`）被 alpha host 消费后所有会话历史加载失败。
- quote: "peer range `>=0.1.0-rc.5 <0.2.0` can never match the alpha line … cache.hydratePrepared is not a function"
- url: https://github.com/deepseek-ai/deepseek-harness/discussions/5450
- source_type: community
- published: unknown (upgrade path 0.1.1-rc.2 → 0.1.2-alpha.5)
- confidence: high

### [6] 2026-09-04 官方升级 0.1.1-rc.2 → 0.1.2-rc.1 事故：升级脚本按 `require.resolve` 返回的 `.pnpm` realpath 原位覆盖 CLI，依赖树新旧混版本（settingsNamespace/installSettingsSection 类错误）；第三方 UI 插件 `@linxin666/dsh-web-ui-all@0.3.4` 绑定旧设置 API 导致 Web 整页起不来；旧回滚只换 CLI 本体不动依赖树、回滚后依旧损坏。
- quote: "升级脚本在错误工作目录执行：`require.resolve` 返回 `.pnpm` 内部 realpath，旧逻辑把新 CLI **原位覆盖进旧的 `.pnpm` 目录**，依赖树新旧混版本"
- url: https://github.com/deepseek-ai/deepseek-harness/discussions/5564
- source_type: community
- published: 2026-09-04 (incident) / discussion after
- confidence: high

### [7] 0.1.2-rc.1 起客户端包被删除/更名（`dsh-client-store`、`dsh-client-ui-primitives`、`dsh-client-ui-slots`），旧插件 0.1.15 仍 require 它们，客户端加载才炸、引导期无提示；社区建议启动/升级时做插件兼容矩阵红黄绿清单。
- quote: "`dsh-client-store`、`dsh-client-ui-primitives`、`dsh-client-ui-slots` 在 0.1.2-rc.1 已不存在，插件 0.1.15 仍 `require` 它们"
- url: https://github.com/deepseek-ai/deepseek-harness/discussions/5609
- source_type: community
- published: 2026-08-17 ~ 2026-09-04 (report window)
- confidence: high

### [8] 0.1.2-alpha 系列四次升级四种故障：rc.7→alpha.1 收紧 `autoInstallPeers:false` + 信任围栏导致秒退；alpha.1→alpha.2 `useSession` 重构 + BrowserAuth 强制 401；alpha.2→alpha.3 框架不再自动注入 core bundles 导致旧 profile 全插件失效；共性是"框架契约在变且 changelog 未标注破坏性变更"。
- quote: "共性是：**不是某个插件不兼容，而是框架自身的契约在变**，且 changelog 未标注为破坏性变更。"
- url: https://github.com/deepseek-ai/deepseek-harness/discussions/5300
- source_type: community
- published: unknown (0.1.2-alpha.1–alpha.3 window)
- confidence: high

### [9] npm `latest` dist-tag 在 `@deepseek-ai/dsh-*` 插件包上冻结在空包 `0.0.1-rc.1`（无 lib/index.js、无 dsh.bundle），`dsh plugin add` 无版本安装即装到坏构建并在 ESM 链接期以误导性"does not provide an export named"报错；根因是 `scripts/release/families.ts:293-295` 对任何含 `-` 的版本只打 `next`、从不推进 `latest`，且 `dsh plugin` 是 `pnpm` 的透传。
- quote: "distTagForVersion returns 'next' for _any_ version containing `-`, and `undefined` (npm's `latest` default) only for stable versions — and the 0.1.x line has never had a non-prerelease publish."
- url: https://github.com/deepseek-ai/deepseek-harness/discussions/5813
- source_type: community
- published: 2026-09-07 (registry measurement in thread)
- confidence: high

### [10] 同一 dist-tag 家族问题至少 8 次独立上报（#1242/#1329/#2233/#2763/#3734/#4222/#4434/#5813），dsh-doctor SP8 实测 8 个插件 × 23 个 peer 包全部 `latest=0.0.1-rc.1` 而 `next=0.1.5-rc.2`，属冻结而非落后；官方 release 脚本门禁只校验 dist-tag 形状、不断言 `latest == newest`。
- quote: "All 8 plugins were flagged: 53 plugin×peer pairs across 23 distinct peer packages, whose `latest` is `0.0.1-rc.1` (20 packages) or `0.0.1-rc.3` (3)."
- url: https://github.com/deepseek-ai/deepseek-harness/discussions/5813
- source_type: community
- published: 2026-09-07
- confidence: high

### [11] 社区已有两套平行的升级门/兼容矩阵方案可对照 dsh-update-copilot 的预检改造：dsh-compat-guard（preflight 门 + storage fingerprint + snapshot/rollback + `dsh.compat` 元数据矩阵，首测 dsh-better-sidebar/dsh-mnemon 在 0.1.0-rc.7→0.1.1-rc.2 通过 8/8）与 dsh-plugin-hub v0.3.25 的「框架升级安全三件套 + 插件适配门」（升级前 `.pnpm` 全树 checkpoint、拉起失败自动全树回滚、不兼容插件强制禁用「待适配」）。
- quote: "dsh-guard preflight — upgrade gate: per-plugin compatibility against the target DSH version … and an automatic $DSH_HOME backup. Blocking changes refuse the upgrade (exit 1)."
- url: https://github.com/deepseek-ai/deepseek-harness/discussions/4487
- source_type: community
- published: 2026-08-25
- confidence: high

### [12] dsh-update-copilot 自身 #8–#13 已落地预检六件套（peer 范围黄色预警含 prerelease 边界、缺失 named export 硬拦截 + force 覆盖、破坏性变更信号解析、更新前快照/回滚即更新/离线 CLI history、全 profile 事后体检 collateral risks、只读 `update_copilot_preflight` 工具），验收评论确认 203 tests green；但 #14 指出 v0.7.0 后台扫描无批次级超时会在 web 就绪瞬间并发约 25 个 npm/github 连接、可把 HTTP 阻塞约 400 秒。
- quote: "lib/background-scan.js 在 web host 就绪时 await scan(true) 全量扫描——任一上游挂起即可阻塞整个启动"
- url: https://github.com/hezhongtang/dsh-update-copilot/issues/14
- source_type: primary
- published: 2026-09-12
- confidence: high

### [13] 热重载缺口是独立兼容面：DSH 内置 `cordis-plugin-hmr` 故意忽略 `node_modules`，插件升级需完整重启才生效；社区 stuarthu/dsh-hot-reload 通过监视 `pnpm-lock.yaml` 在原位热替换，失败时保留旧版并要求手动重启（bundle patch / lockfile 变更时序会影响「first lockfile write after boot」的重载语义）。
- quote: "dsh's built-in hot-reload (`cordis-plugin-hmr`) deliberately ignores `node_modules`, so upgrading an installed plugin … normally requires a full `dsh` restart to take effect."
- url: https://github.com/stuarthu/dsh-hot-reload
- source_type: community
- published: unknown (around 2026-08-20)
- confidence: high

### [14] 官方 v0.1.2-rc.1 release notes 把 oh-my-dsh/dsh-plugin-upgrade-skill 写进推荐：该仓库维护 127 张升级说明卡（0.1.0-rc.8→0.1.5-rc.2），按版本记录真实破坏点，可作为 dsh-update-copilot 破坏性变更信号/兼容模型的结构化知识源。
- quote: "oh-my-dsh 开源社区推出了帮助插件作者随着 DSH 版本升级插件代码的 skill： https://github.com/oh-my-dsh/dsh-plugin-upgrade-skill"
- url: https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.2-rc.1
- source_type: primary
- published: 2026-09-03
- confidence: high

## Dead ends
- `gh search issues "dsh plugin compatibility OR export OR upgrade"` 在公网搜索面几乎为空（deepseek-harness 关闭了 Issues，问题都在 Discussions）。
- 对 `deepseek-ai/deepseek-harness` 的 `gh issue list` 直接失败：`the 'deepseek-ai/deepseek-harness' repository has disabled issues`。
- web 搜 "dsh plugin upgrade gate rollback" 主要落到 SEO/插件市场聚合页（apimodels.app、springbrand.ai、atoms.dev 等），未采信。
- web 搜 "export lost OR removed rename plugin ESM link failure" 被无关 "failure" 主题污染，零有效结果。
- dsh-update-copilot 的 releases API 返回空数组（该仓库尚未打 GitHub Release）。
- oh-my-dsh/dsh-plugin-upgrade-skill 的 `cards/` 目录不存在（卡片实际在 skills/ 等子树），未展开 127 张卡明细。

## Suggested follow-ups
- 对照阅读 oh-my-dsh/dsh-plugin-upgrade-skill 的 127 张升级卡版本分布，验证 dsh-update-copilot 的破坏性变更标记集（feat!/BREAKING/移除/更名）能否覆盖其中高频模式（导出删除、包更名、core bundle 注入策略、autoInstallPeers）。
- 评估 dsh-update-copilot 预检是否应吸收 dsh-compat-guard 的 storage-format fingerprint 与 dsh-plugin-hub 的「全树 checkpoint / 拉起失败回滚」——当前 #11 快照只含单包旧版本/patch 指纹，不含 `.pnpm` 全树。
- 检视 bundle patch 挂载关系（PR #1）与 stuarthu/dsh-hot-reload 的 lockfile-watch 重载语义在「更新后 first lockfile write」上的交互，避免热重载与 copilot 更新执行器并发 mutate 同一 lock。
